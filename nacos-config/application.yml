spring: 
  # 数据库
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://124.222.46.195:3306/magic_repo_kit?useSSL=false&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&tinyInt1isBit=false&allowMultiQueries=true&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: apollotest
  # redis
  redis:
    ##redis 单机环境配置
    host: 124.222.46.195
    port: 6379
    password: apollotest
    database: 0
    ssl: false
  # spring缓存
  cache:
    type: redis
    redis:
      time-to-live: 1h
  mvc:
    pathmatch:
      # 解决 SpringFox 与 SpringBoot 2.6.x 不兼容的问题，参见 SpringFoxHandlerProviderBeanPostProcessor 类
      matching-strategy: ANT_PATH_MATCHER
    
# GPT服务配置
gpt:
  token:
    sing-key: gptgptisapowerfulmicroservicearchitectureupgradedandoptimizedfromacommercialproject
  auth:
    local:
      client-id: gpt
      client-secret: gpt
  langchain:
    elastic-username: elastic
    elastic-password: 123456
    elastic-host: 154.204.60.125
    elastic-port: 9200

# 三方登录配置
justauth:
  cache:
    prefix: 'gpt:social:state:'
    type: redis
    timeout: 24h
  enabled: true
  type:
    GITEE:
      client-id: 自己申请
      client-secret: 自己申请
      redirect-uri: 自己登陆地址
      ignore-check-redirect-uri: true

# mybatis日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 对象存储
oss:
  enabled: false