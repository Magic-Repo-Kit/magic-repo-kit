server:
  port: 1000

spring:
  application:
    name: gpt-gateway
  config:
    activate:
      on-profile: pro
  cloud:
    # 注册中心配置
    nacos:
      discovery:
        server-addr: 182.92.129.14
      config:
        server-addr: 182.92.129.14
        file-extension: yml
        shared-configs[0]:
          dataId: application.yml
          refresh: true
