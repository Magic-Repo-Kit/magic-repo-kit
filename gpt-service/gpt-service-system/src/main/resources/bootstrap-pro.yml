server:
  port: 8001
spring:
  application:
    name: gpt-system
  config:
    activate:
      on-profile: pro
  # 配置中心
  cloud:
    # 注册中心配置
    nacos:
      discovery:
        server-addr: 182.92.129.14
      config:
        server-addr: 182.92.129.14
        # 配置文件格式
        file-extension: yml
        shared-configs[0]:
          dataId: application.yml
          refresh: true

