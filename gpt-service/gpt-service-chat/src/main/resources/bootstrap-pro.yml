server:
  port: 8002
spring:
  application:
    name: gpt-chat
  config:
    activate:
      on-profile: pro
  # 配置中心
  cloud:
    # 注册中心配置
    nacos:
      discovery:
        server-addr: 182.92.129.14:8848
      config:
        server-addr: 182.92.129.14:8848
        file-extension: yml
        shared-configs[0]:
          dataId: application.yml
          refresh: true